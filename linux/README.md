# linux/

Rust + GTK4/Libadwaita host application. Owns the QUIC endpoint, GStreamer decode pipeline, input capture, the FUSE virtual mount, and the Libadwaita UI.

**Status:** 📋 not yet implemented. See `docs/SYSTEM_DESIGN.md`.
