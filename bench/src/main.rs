// HyperLink bench — measurement harness entry point.
// This is the first thing to build (Phase 0): clock-sync handshake + round-trip echo.

fn main() {
    println!("HyperLink bench — scaffold only, Phase 0 in progress.");
    println!("Target: clock-sync handshake + round-trip echo, see docs/SYSTEM_DESIGN.md");
}
